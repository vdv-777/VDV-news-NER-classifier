# 🌐 NEREL Нейросетевой сервис распознавания сущностей и событий в текстах

![NEREL](https://img.shields.io/badge/dataset-NEREL-blue) ![Python 3.12](https://img.shields.io/badge/python-3.12-green) ![PyTorch](https://img.shields.io/badge/framework-PyTorch-orange)

Проект реализует **многоцелевую модель** для одновременного извлечения именованных сущностей (NER) и событий (relation extraction) на русскоязычном датасете **[Скачать по ссылке] https://huggingface.co/datasets/iluvvatar/NEREL**.  
Поддерживается **инференс с квантизацией**, **гибкая настройка через конфиг** и **полная воспроизводимость всего сервиса с нуля**. Для упрощения разработки и отладки - весь пайплайн подготовки данных, обучения и работы модели в режиме классификации реализованы в одном Jupyter-ноутбуке!

---

## 🎯 Что делает эта модель?

### 🔍 Решаемые задачи
1. **Извлечение именованных сущностей (Named Entity Recognition, NER)**  
   → Распознаёт 29 типов сущностей, включая:
   - Люди (`PERSON`), Организации (`ORGANIZATION`)
   - Города (`CITY`), Страны (`COUNTRY`)
   - События (`EVENT`), Даты (`DATE`), Деньги (`MONEY`), Профессии (`PROFESSION`) и др.

2. **Извлечение событий (Relation Classification)**  
   → Определяет **15 типов отношений** между сущностями в документе:
   - `WORKS_AS` (работает как), `LOCATED_IN` (расположен в),
   - `ALTERNATIVE_NAME` (альтернативное имя), `AGE_IS` и др.

> 💡 Модель решает **две задачи в одном проходе**, что повышает эффективность.
>
> ---

## 🌍 Сферы применения

- **Аналитика новостей** — автоматическое структурирование статей.
- **Интеллектуальный поиск** — поиск по сущностям, а не по словам.
- **Базы знаний** — автоматическое наполнение графов знаний.
- **Мониторинг СМИ** — отслеживание упоминаний компаний, лиц, событий.
- **Поддержка принятия решений** — выявление ключевых фактов из текстов.

---

## 📁 Структура проекта

vdv-ner/

├── NEREL_start.ipynb # Основной Jupyter Notebook

├── data/ # Датасет (train.jsonl, dev.jsonl итд)

├── inf/ # Папка с текстами для инференса (*.txt)

├── requirements.txt # Зависимости

├── README.md # Этот файл

└── best_model_top15/ # 📦 Папка с весами модели (НЕ в репозитории)


> ⚠️ **Веса обученной модели не включены в репозиторий** из-за размера (~500 МБ).  
> ➤ **Скачать веса** можно по ссылке с облака автора
> **[Скачать по ссылке] https://drive.google.com/drive/folders/1Iy4xBnqhTwwr2IO4VdSYDlmMu8ivdhO3?usp=sharing**

> ➤ Или **обучите модель сами** — всё для этого есть в ноутбуке!
>
> ---

## 📘 Описание блоков ноутбука

| Блок | Назначение |
|------|----------|
| **1. Конфигурация** | Централизованная настройка всех параметров обучения и инференса |
| **2. Импорты и устройство** | Определение `DEVICE`, воспроизводимость (`seed=43`) |
| **3. Парсинг NEREL** | Загрузка и преобразование `train.jsonl` → сущности + события |
| **4. Токенизация** | Корректное выравнивание меток с субтокенами через `DataCollatorForTokenClassification` |
| **5. Модель** | Определение `JointModel` (BERT + две головы: NER и CLS) |
| **6. Обучение** | Цикл обучения с сохранением лучшей модели по `combined = (NER_F1 + CLS_F1)/2` |
| **7. Инференс** | **Универсальный блок**: краткий/детальный вывод, квантизация, сравнение скорости |

---

## ⚙️ Основные параметры конфигурации модели (`CONFIG`)

| Параметр | Описание | Пример |
|--------|--------|--------|
| `model_name` | Имя предобученной модели | `"DeepPavlov/rubert-base-cased"` |
| `num_epochs` | Число эпох обучения | `10` |
| `learning_rate` | Скорость обучения | `2e-5` |
| `use_uncertainty_weighting` | Автоматический баланс loss'ов | `True` |
| `inference_mode` | Режим вывода | `"short"` / `"detailed"` |
| `use_quantization` | Использовать квантизацию (только на CPU!) | `False` |
| `run_quantization_comparison` | Сравнить скорость с/без квантизации | `False` |
| `output_dir` | Папка для сохранения модели | `"best_model_top15"` |
| `inference_dir` | Папка с текстами для инференса | `"inf"` |

> 💡 Все гиперпараметры **настраиваются в одном месте** — в начале ноутбука.
>
> ### ☁️ Подготовка ВМ для развертывания

**1) Скачиваем датасет для обучения**
(если планируем использовать модель в режиме обучения)

**2) Устанавливаем Python-3.10.12**
wget https://www.python.org/ftp/python/3.10.12/Python-3.10.12.tgz
tar -xf Python-3.10.12.tgz
cd Python-3.10.12

./configure --enable-optimizations
make -j$(nproc)
sudo make altinstall

python3.10 --version

python3.10 -m venv p310p2

source mp310p2/bin/activate

**3) Устанавливаем зависимости, включая ipykernel и jupyterlab**
pip3 install -r requirements.txt

При локальном развертывании без удаленных ВМ - пункты 4-7 можно пропустить.

**4) При глобальной установке пакетов без sudo - сохраняем путь к установленным пакетам в path ( чтобы python мог успешно найти все наши пакеты)**

export PATH="$HOME/.local/bin:$PATH"

echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc

**5) Запускаем на локалхосте сервер jupyterlab**

jupyter-lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root
[I 2025-11-17 14:57:40.023 ServerApp]     http://127.0.0.1:8888/lab?token=your_token_here

**6) Открываем порты для доступа jupyterlab через обратный портфорвардинг**
### На Windows (в PowerShell или CMD)
ssh -L 8888:localhost:8888 vdv777@158.160.154.189

**7) Открываем jupyterlab в браузере, авторизуемся по полученному токену**
http://localhost:8888
**8) Можно приступать к запуску ноутбуков на ВМ с jupyterlab.**

---

## 🚀 Руководство пользователя

### 1. **Подготовка**
- Скачайте папку с файлами этого репозитория
### 2. Подготовка данных
Поместите train.jsonl и dev.jsonl из NEREL в папку data/.
Добавьте свои .txt файлы в inf/ для инференса.
### 3. Выбор сценария
📌 **Вариант A: Использовать готовую модель**
- Скачиваем папку с авторской моделью best_model_top15/.
- Распакуйте папку в корень проекта.
- В CONFIG установите:
  "inference_mode": "detailed",
"use_quantization": True  # или False
- Запустите последовательно все блоки, кроме блока обучения.
  
📌 **Вариант B: Обучить модель с нуля**
Убедитесь, что data/train.jsonl и data/dev.jsonl на месте.
В CONFIG настройте параметры обучения (рекомендуется num_epochs=10, learning_rate=2e-5).
Запустите все блоки подряд (1 → 7).
После обучения запустите инференс (блок 7).


---

## 🏆 Достигнутые результаты

| Метрика | Значение |
|--------|----------|
| **NER F1 (token-level)** | **0.678** ✅ |
| **CLS F1 (events)** | **0.857** ✅ |
| **Ускорение с квантизацией** | **1.38x** (0.623 → 0.451 сек/текст) |
| **Качество при квантизации** | **Практически идентично** — расхождения единичны |

### 🔍 Ключевые наблюдения:
- Модель **успешно извлекает сложные сущности** (география, профессии, события).
- **Квантизация безопасна**: не ухудшает качество, но ускоряет инференс на CPU.
- Ошибки в основном связаны с **особенностями разметки NEREL** (например, слова `«президент» → PROFESSION`), а не с дефектами модели.

---

## 🌱 Дальнейшее возможное развитие

- **Адаптация под другие домены** (медицина, финансы) через дообучение.
- **Веб-сервис** (Gradio/Streamlit) для интерактивного инференса.
- **Экспорт** для развёртывания в production.

---
